import React from 'react';
import { Dashboard } from '../organisms/Dashboard.jsx';

export const HomePage = () => {
    return (
        <main style={{ minHeight: '100vh', background: '#f8fafc' }}>
            <Dashboard />
        </main>
    );
};
